import numpy as np
height_men = 170
height_std = 5 # normal 
heights = np.random.normal(height_men,height_std,size=(3 , 1000000))
print(heights.mean(),heights.std())
scores = np.random.uniform(low = 10 , high = 20 , size = 50)
print(scores)

